name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    uses: ./.github/workflows/test-reusable.yml
    with:
      ruby-versions: '["3.2", "3.3", "3.4"]'
      lint: false
      coverage: false
